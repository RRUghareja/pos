{
  "name": "pos-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "admin-panel"
  ],
  "dependencies": {
    "dotenv": "^16.4.5",
    "serverless-http": "^3.2.0"
  },
  "scripts": {
    "build": "cd backend && npx prisma generate && cd ../admin-panel && npm run build",
    "dev:backend": "cd backend && npm run dev",
    "dev:admin": "cd admin-panel && npm run dev"
  }
}
